{
  "name": "friendsfeed",
  "version": "0.0.1",
  "description": "A social network for friends to see life updates only to support friends communication without influencers, creators, pages, etc...",
  "author": "Amr2812",
  "main": "./core/package.json",
  "scripts": {
    "preinstall": "cd ./core && npm ci --include=dev && npm run build && npm run start:prod"
  }
}
